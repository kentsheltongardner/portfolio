export default class ColorStop {
    position:   number
    color:      string
    constructor(position: number, color: string) {
        this.position   = position
        this.color      = color
    }
}