export default class Constellation {
    big_stars;
    connections;
    period;
    constructor(big_stars, connections, period) {
        this.big_stars = big_stars;
        this.connections = connections;
        this.period = period;
    }
}
